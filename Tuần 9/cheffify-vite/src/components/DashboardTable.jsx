import { useState } from 'react';import { Edit3 } from 'lucide-react';import { customers } from '../data/data';
export default function DashboardTable(){const [q,setQ]=useState('');const [sel,setSel]=useState([]);const [edit,setEdit]=useState('');const rows=customers.filter(c=>(c.name+c.company).toLowerCase().includes(q.toLowerCase()));const toggle=id=>setSel(s=>s.includes(id)?s.filter(x=>x!==id):[...s,id]);return <div className="report"><div className="report-top"><h2>Detailed report</h2><input placeholder="Search customer..." value={q} onChange={e=>setQ(e.target.value)}/><button onClick={()=>setEdit('Import mode ready')}>Import</button><button onClick={()=>setEdit('Exported selected rows: '+sel.length)}>Export</button></div><table><thead><tr><th><input type="checkbox" onChange={e=>setSel(e.target.checked?rows.map(r=>r.id):[])}/></th><th>Customer name</th><th>Company</th><th>Order value</th><th>Order date</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><input checked={sel.includes(r.id)} onChange={()=>toggle(r.id)} type="checkbox"/></td><td><img src={r.avatar}/> {r.name}</td><td>{r.company}</td><td>{r.value}</td><td>{r.date}</td><td><span className={'badge '+r.status.toLowerCase().replace('-','')}>{r.status}</span></td><td><button className="icon" onClick={()=>setEdit('Editing '+r.name)}><Edit3 size={16}/></button></td></tr>)}</tbody></table><p className="status">{edit}</p><div className="pagination"><button>‹</button><button className="active">1</button><button>2</button><button>3</button><button>4</button><button>...</button><button>10</button><button>11</button><button>›</button></div></div>}
