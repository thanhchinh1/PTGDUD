import { useState } from 'react';
export default function FilterSidebar({onApply}){const [types,setTypes]=useState(['Grilled','Roasted']); const [time,setTime]=useState(50); const [rating,setRating]=useState(3); const toggle=t=>setTypes(v=>v.includes(t)?v.filter(x=>x!==t):[...v,t]); return <aside className="filters"><h3>☰ FILTERS</h3><section><b>Type</b>{['Pan-fried','Stir-fried','Grilled','Roasted','Sauteed','Baked','Steamed','Stewed'].map(t=><label key={t}><input type="checkbox" checked={types.includes(t)} onChange={()=>toggle(t)}/>{t}</label>)}</section><section><b>Time</b><input type="range" min="10" max="80" value={time} onChange={e=>setTime(e.target.value)}/><small>{time} minutes</small></section><section><b>Rating</b>{[5,4,3,2,1].map(r=><label key={r}><input name="rating" type="radio" checked={rating===r} onChange={()=>setRating(r)}/><span className="stars">{'★'.repeat(r)}{'☆'.repeat(5-r)}</span></label>)}</section><button className="primary full" onClick={()=>onApply?.({types,time,rating})}>Apply</button></aside>}
