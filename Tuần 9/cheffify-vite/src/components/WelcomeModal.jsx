import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { X } from 'lucide-react';import { img } from '../data/data';
export default function WelcomeModal(){const [slide,setSlide]=useState(0);const nav=useNavigate();const next=()=> slide<2?setSlide(slide+1):nav('/login');return <div className="overlay"><div className="welcome modal"><button className="x" onClick={()=>nav('/home')}><X/></button><h1>Discover Cheffify</h1><p>Easy and delicious cooking instructions right here. Start exploring now!</p><img src={img.modal}/><div className="dots">{[0,1,2].map(i=><b key={i} className={slide===i?'on':''}/>)}</div><button className="primary" onClick={next}>{slide<2?'Next':'Go Login'}</button><button className="linkbtn" onClick={()=>nav('/home')}>Skip</button></div></div>}
