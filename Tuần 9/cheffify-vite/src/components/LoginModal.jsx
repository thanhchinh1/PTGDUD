import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { X } from 'lucide-react';import { img } from '../data/data';
export default function LoginModal(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');const nav=useNavigate();const cont=()=>{/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email)?(setMsg('Logged in demo successfully.'),setTimeout(()=>nav('/home'),600)):setMsg('Please enter a valid email.');};return <div className="overlay"><div className="login-modal"><div className="login-art" style={{backgroundImage:`url(${img.login})`}}>"Embrace the art of cooking, where flavors come alive!"</div><div className="login-form"><button className="x" onClick={()=>nav('/home')}><X/></button><h1>Login</h1><p>Enter your email to log in.</p><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Enter your email"/><button className="primary full" onClick={cont}>Continue</button><div className="or">OR</div><small>By continuing, you agree to Terms of Sale, Terms of Service, and Privacy Policy.</small>{['Google','Facebook','Apple'].map(x=><button className="social" key={x} onClick={()=>setMsg(`Continue with ${x} selected`) }>Continue with {x}</button>)}<p className="status">{msg}</p></div></div></div>}
