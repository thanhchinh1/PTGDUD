import Header from '../components/Header';import Footer from '../components/Footer';import RecipeCard from '../components/RecipeCard';import FilterSidebar from '../components/FilterSidebar';import Pagination from '../components/Pagination';import { recipes } from '../data/data';import { useState } from 'react';
export default function Salad(){const [sort,setSort]=useState('az');const [applied,setApplied]=useState('');const list=[...recipes.slice(4,13)].sort((a,b)=>sort==='az'?a.title.localeCompare(b.title):b.title.localeCompare(a.title));return <><Header/><main className="page-width listing min1696"><FilterSidebar onApply={f=>setApplied(`Filter applied: ${f.types.length} types, ${f.time} min`)}/><section className="list-content"><div className="list-head"><h1>Salad (32)</h1><select value={sort} onChange={e=>setSort(e.target.value)}><option value="az">A-Z</option><option value="za">Z-A</option></select></div><p className="status">{applied}</p><div className="grid3">{list.map(r=><RecipeCard key={r.id} recipe={r}/>)}</div><Pagination/></section></main><Footer/></>}
