import Header from '../components/Header';import Footer from '../components/Footer';import RecipeCard from '../components/RecipeCard';import Pagination from '../components/Pagination';import Breadcrumb from '../components/Breadcrumb';import { recipes, img } from '../data/data';import { useState } from 'react';
export default function RecipeBox(){const [tab,setTab]=useState('Saved Recipes');const [msg,setMsg]=useState('');return <><Header/><Breadcrumb items={['Your Recipe Box']}/><main className="page-width profile min1693"><h1>Emma Gonzalez's Recipe Box</h1><div className="profile-row"><img src={img.avatar}/><p>Emma Gonzalez is a deputy editor at Cheffify, bringing her expertise as a former cooking editor at the Los Angeles Times. She is also an accomplished author and now resides in New York City.</p></div><button className="textpink">6.5k Subscribes</button><button className="primary small" onClick={()=>setMsg('Share link copied')}>Share ↗</button><div className="tabs">{['Saved Recipes','Folders','Recipes by Genevieve'].map(t=><button className={tab===t?'active':''} onClick={()=>setTab(t)}>{t}</button>)}</div><p className="status">{msg||tab}</p><div className="grid4 profile-grid">{recipes.slice(0,8).map(r=><RecipeCard key={r.id} recipe={r}/>)}</div><Pagination/></main><Footer/></>}
