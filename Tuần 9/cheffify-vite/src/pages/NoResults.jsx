import Header from '../components/Header';import Footer from '../components/Footer';import FilterSidebar from '../components/FilterSidebar';import { useState } from 'react';import { img } from '../data/data';
export default function NoResults(){const [state,setState]=useState('No filter applied');return <><Header/><main className="page-width listing min1490"><FilterSidebar onApply={f=>setState(`Applied: ${f.types.join(', ')} • ${f.time} min • ${f.rating} stars`)}/><section className="empty"><h1>Sorry, no results were found for “cakescacsca”</h1><img src={img.empty}/><p>We have all your Independence Day sweets covered.</p><div><b>Sweet Cake</b><b>Black Cake</b><b>Pozole Verde</b><b>Healthy food</b></div><small>{state}</small></section></main><Footer/></>}
